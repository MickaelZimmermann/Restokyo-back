{% extends 'base.html.twig' %}

{% block title %}{{ tag.name }}{% endblock %}

{% block body_back %}

    <h1>{{ tag.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag.name }}</td>
            
        </tbody>
    </table>

    <a href="{{ path('app_back_tag') }}" class="btn btn-info">Retour à la liste des spécialités</a>
    
    <a href="{{ path('back_tag_edit', {'id': tag.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('tag/_delete_form.html.twig') }}

{% endblock %}
