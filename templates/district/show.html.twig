{% extends 'base.html.twig' %}

{% block title %}{{ district.name }}{% endblock %}

{% block body_back %}

    <h1>{{ district.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ district.name }}</td>
            </tr>
            <tr>
                <th>Kanji</th>
                <td>{{ district.kanji }}</td>
        </tbody>
    </table>

    <a href="{{ path('back_district_index') }}" class="btn btn-info">Retour à la liste des quartiers</a>
    
    <a href="{{ path('back_district_edit', {'id': district.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('district/_delete_form.html.twig') }}

{% endblock %}
