{% extends 'base.html.twig' %}

{% block title %}Modifier un quartier{% endblock %}

{% block body_back %}
    <h1>Modifier un quartier</h1>

    {{ include('district/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a href="{{ path('back_district_index') }}" class="btn btn-info">Retour à la liste des quartiers</a>

    {{ include('district/_delete_form.html.twig') }}
{% endblock %}
